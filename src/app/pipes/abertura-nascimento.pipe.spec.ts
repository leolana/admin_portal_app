import { AberturaNascimentoPipe } from './abertura-nascimento.pipe';

describe('AberturaNascimentoPipe', () => {
  it('create an instance', () => {
    const pipe = new AberturaNascimentoPipe();
    expect(pipe).toBeTruthy();
  });
});
