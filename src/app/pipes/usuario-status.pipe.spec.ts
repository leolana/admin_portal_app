import { UsuarioStatusPipe } from './usuario-status.pipe';

describe('UsuarioStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new UsuarioStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
