import { TipoPessoaPipe } from './tipo-pessoa.pipe';

describe('TipoPessoaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoPessoaPipe();
    expect(pipe).toBeTruthy();
  });
});
