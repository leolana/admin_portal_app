import { NumeroCartaoPipe } from './numero-cartao.pipe';

describe('NumeroCartaoPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroCartaoPipe();
    expect(pipe).toBeTruthy();
  });
});
