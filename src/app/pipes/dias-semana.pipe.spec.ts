import { DiasSemanaPipe } from './dias-semana.pipe';

describe('DiasSemanaPipe', () => {
  it('create an instance', () => {
    const pipe = new DiasSemanaPipe();
    expect(pipe).toBeTruthy();
  });
});
