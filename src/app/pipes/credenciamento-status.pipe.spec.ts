import { CredenciamentoStatusPipe } from './credenciamento-status.pipe';

describe('CredenciamentoStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CredenciamentoStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
