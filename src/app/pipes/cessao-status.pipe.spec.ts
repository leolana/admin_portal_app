import { CessaoStatusPipe } from './cessao-status.pipe';

describe('CessaoStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CessaoStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
