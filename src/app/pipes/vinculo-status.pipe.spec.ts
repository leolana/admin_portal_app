import { VinculoStatusPipe } from './vinculo-status.pipe';

describe('VinculoStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new VinculoStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
