import { TipoCapturaPipe } from './tipo-captura.pipe';

describe('TipoCapturaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoCapturaPipe();
    expect(pipe).toBeTruthy();
  });
});
